{
  "name": "retail-market-root",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r exec tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
